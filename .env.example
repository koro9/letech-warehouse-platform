# ---- 后端 API 地址 ----
# 生产：通过 nginx 同域代理（保留默认 /api 即可）
# 本地开发：vite 配置已自动代理 /api → VITE_DEV_API_TARGET
VITE_API_BASE_URL=/api

# ---- 仅本地开发 ----
# Vite dev server 把 /api 代理到这个地址
VITE_DEV_API_TARGET=http://localhost:8069
